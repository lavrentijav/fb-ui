---
title: Network
description: 'The topology graph and the cascade links drawn on it. The graph is
  a projection, not a second source of truth: vertices are registered panels
  (this one plus every row in the node registry) and each panel carries the
  inbounds whose ownership column points at it. A cascade link is the stored
  form of what an operator otherwise assembles by hand — an outbound plus a
  routing rule on the source panel. All endpoints under /panel/api/network.'
full: true
_openapi:
  preload:
    - ./public/openapi.json
  toc:
    - depth: 2
      title: 'Return the whole topology in one response: every panel with its
        inbounds, plus the cascade links between them. Panel id 0 is the panel
        serving the request.'
      url: '#return-the-whole-topology-in-one-response-every-panel-with-its-inbounds-plus-the-cascade-links-between-them-panel-id-0-is-the-panel-serving-the-request'
    - depth: 2
      title: Draw one cascade edge. The source inbound must live on the source panel
        and the target inbound on the target panel; the two panels must differ.
        Stored pending, then materialized into the source panel’s Xray config.
      url: '#draw-one-cascade-edge-the-source-inbound-must-live-on-the-source-panel-and-the-target-inbound-on-the-target-panel-the-two-panels-must-differ-stored-pending-then-materialized-into-the-source-panels-xray-config'
    - depth: 2
      title: Remove a cascade edge and re-push the source panel.
      url: '#remove-a-cascade-edge-and-re-push-the-source-panel'
    - depth: 2
      title: Pause or resume one cascade edge without deleting it.
      url: '#pause-or-resume-one-cascade-edge-without-deleting-it'
  structuredData:
    headings:
      - content: 'Return the whole topology in one response: every panel with its
          inbounds, plus the cascade links between them. Panel id 0 is the panel
          serving the request.'
        id: return-the-whole-topology-in-one-response-every-panel-with-its-inbounds-plus-the-cascade-links-between-them-panel-id-0-is-the-panel-serving-the-request
      - content: Draw one cascade edge. The source inbound must live on the source panel
          and the target inbound on the target panel; the two panels must
          differ. Stored pending, then materialized into the source panel’s Xray
          config.
        id: draw-one-cascade-edge-the-source-inbound-must-live-on-the-source-panel-and-the-target-inbound-on-the-target-panel-the-two-panels-must-differ-stored-pending-then-materialized-into-the-source-panels-xray-config
      - content: Remove a cascade edge and re-push the source panel.
        id: remove-a-cascade-edge-and-re-push-the-source-panel
      - content: Pause or resume one cascade edge without deleting it.
        id: pause-or-resume-one-cascade-edge-without-deleting-it
    contents: []
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./public/openapi.json" webhooks={[]} operations={[{"path":"/panel/api/network/graph","method":"get"},{"path":"/panel/api/network/link","method":"post"},{"path":"/panel/api/network/link/del/{id}","method":"post"},{"path":"/panel/api/network/link/setEnable/{id}","method":"post"}]} showTitle />
    </>
  );
}